{
  "name": "@geolibre/gis-server",
  "version": "0.1.0",
  "private": true,
  "description": "GIS 服务端：场景 CRUD、二维/三维数据发布、缓存托管与管理页（Express + TypeScript，文件存储）",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit && esbuild src/index.ts --bundle --platform=node --format=esm --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --outfile=dist/index.js",
    "start": "node dist/index.js",
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^2.0.1",
    "@geolibre/gis-shared": "*"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0",
    "esbuild": "^0.25.0"
  }
}
